# Asterisk Config Templates

Os arquivos reais são gerados por `scripts/install-asterisk.sh` diretamente em
`/etc/asterisk` e `/etc/odbc.ini`.

Esta pasta existe para manter o mesmo layout dos demais engines e documentar que a
configuração canônica do Asterisk é gerada pelo instalador.
